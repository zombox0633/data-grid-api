import { RouteShorthandOptions } from "fastify";

const OptsTest: RouteShorthandOptions = {
  schema: {
    response: {
      200: {
        type: 'object',
        properties: {
          pong: {
            type: 'string'
          }
        }
      }
    }
  }
};

export default OptsTest